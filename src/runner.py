🧱 GOAL:
Create a script (CLI-based to start) that:

Takes a user input (prompt)

Sends it to an LLM (like OpenAI’s GPT-4)

Receives the output

Logs the result to the database with version metadata